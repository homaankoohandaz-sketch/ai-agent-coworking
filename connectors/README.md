# Connector Layer

Connector types:
- MCP
- HTTP API
- CLI
- GitHub Actions
- webhook

Every connector declares provider, capability, input schema, output schema, auth method, permission scope, health check and retry policy.

MCP is preferred when it provides a stable tool contract.
